# Loop Prevention

- If the same approach fails twice, stop and re-evaluate before retrying
- After 3 consecutive tool errors on the same file, pause and explain the pattern to the user
- If a build/test still fails after a fix, verify the fix addressed the actual error message before retrying
