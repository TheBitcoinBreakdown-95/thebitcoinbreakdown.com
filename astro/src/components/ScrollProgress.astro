---
/**
 * Scroll progress bar — fixed at top, width calculated from scroll position.
 * Already included in BaseLayout.astro as a div. This component exists as
 * a standalone option if needed outside the base layout.
 */
---

<div class="scroll-progress" id="scrollProgress" aria-hidden="true"></div>
